<%= render 'shared/menu' %>

<% if @artist %>
<h1> Bio <br>
  <% if @artist.name %>
  <%= @artist.name %>
  <% else %>
  <%= p 'Artist name not found' %>
  <% end %>
</h1>

<p>
  <% if @artist.biography %>
  <%= @artist.biography %>
  <% else %>
  <%= p 'Artist bio not found' %>
  <% end %>
</p>

<% else %>
<h1>
  <%= p 'Artist not found'%>
</h1>
<% end %>
