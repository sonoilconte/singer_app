<!-- Static navbar -->
<nav class="navbar navbar-default navbar-static-top">
  <%= link_to 'View website', '/', class: 'navbar-brand' %>
  <div class="container">
    <div class="navbar-header">
       <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
         <span class="sr-only">Toggle navigation</span>
         <span class="icon-bar"></span>
         <span class="icon-bar"></span>
         <span class="icon-bar"></span>
       </button>
   </div>
   <div id="navbar" class="navbar-collapse collapse">
     <ul class="nav navbar-nav admin-navbar">
       <% if !@current_user %>
       <%= nav_link "Log in", "/login" %>
       <% else %>
       <%= nav_link "Edit Info", "/artists/#{ENV['PRIMARY_USER_ID']}/edit" %>
       <%= nav_link "Edit Images", "/artists/#{ENV['PRIMARY_USER_ID']}/images" %>
       <%= nav_link "Edit Events", "/artists/#{ENV['PRIMARY_USER_ID']}/events" %>
       <% end %>
     </ul>
     <ul class="nav navbar-nav navbar-right admin-navbar">
       <%= nav_link "Log out", "/logout" %>
     </ul>
     <span class="navbar-text navbar-right">
       <% if @current_user %>
       <div> Welcome, <%= @current_user[:name] %>!  </div>
       <% end %>
     </span>
   </div>
 </div>
</nav>
